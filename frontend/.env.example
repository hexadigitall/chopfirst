# API base URL — set this to your deployed backend URL (e.g. https://your-app.vercel.app)
# Leave empty for development (uses Vite proxy to localhost:3001)
VITE_API_URL=

# Google OAuth — from Google Cloud Console OAuth client (Web application)
VITE_GOOGLE_CLIENT_ID=
